# File viewer
A simple file viewer written in C using SDL2 set of libraries (check out `deps` directory)

## Setup
```
git clone https://github.com/reymons/file-viewer.git --recursive
cd file-viewer
make init
```
## Compile and run
```
make
./main -d [dir_to_files]
```

